# 用法：cp .env.example .env 然后填入真实值
# .env 已在 .gitignore 里，不会被提交；这个 .example 文件是给人看的模板。

# Product Hunt Developer Token
# 获取：登录 producthunt.com 后打开 https://api.producthunt.com/v2/oauth/applications
# 点 "Add application"（名字和 redirect URL 随便填），复制页面上的
# **Developer Token** —— 不是 Client Secret，两个不一样。
# 不填也能跑，Product Hunt 那一段会自动跳过。
PH_API_TOKEN=

# GitHub Personal Access Token（可选）
# 不填也能跑，只是搜索接口限额从 30 次/分 降到 10 次/分，11 个品类会触发限流。
# 去 https://github.com/settings/tokens 建一个 classic token，
# 不需要勾选任何权限（只是提高公开数据的读取限额）。
GITHUB_TOKEN=
